import 'package:flutter/material.dart';

class Tela2Page extends StatelessWidget {

  const Tela2Page({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Tela2'),),
           body: Container(),
       );
  }
}